function toggle2() {
    const product = document.querySelector('.product-field'); // Choosing the element
    if (product) {
        product.classList.toggle("active"); // Turning the active
    } 
}
